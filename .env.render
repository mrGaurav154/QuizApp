# Server Configuration for Render
PORT=3000
NODE_ENV=production

# MongoDB Configuration (use your Render environment variable)
# On Render, set this as an environment variable: MONGODB_URI
MONGODB_URI=${MONGODB_URI}

# Session Configuration
SESSION_SECRET=${SESSION_SECRET}

# JWT Configuration (kept for backward compatibility)
JWT_ACCESS_SECRET=${JWT_ACCESS_SECRET}
JWT_REFRESH_SECRET=${JWT_REFRESH_SECRET}
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# Frontend URL (for CORS) - Update to your Render domain
CLIENT_URL=https://your-app.onrender.com

# Base URL for quiz join links
BASE_URL=https://your-app.onrender.com
